{
  "name": "free-time-matcher-root",
  "version": "1.0.0",
  "type": "module",
  "description": "Root project for Course Free-Time Matcher",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
